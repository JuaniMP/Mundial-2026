# Data Model: API REST Mundial 2026 Hub

## Entity Relationships

```
┌─────────────┐       ┌─────────────┐
│     Rol     │       │   Usuario   │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ nombre      │  └───►│ id_rol (FK) │
└─────────────┘       │ email       │
                      │ password    │
                      └─────────────┘

┌─────────────┐       ┌─────────────┐
│     Sede    │       │  Estadio    │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ ciudad      │  └───►│ id_sede (FK)│
│ pais        │       │ capacidad   │
└─────────────┘       └─────────────┘

┌─────────────┐       ┌─────────────┐
│  Seleccion  │       │  Jugador    │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ pais        │  └───►│ id_seleccion│
│ codigo_fifa │       │ posicion    │
│ confederacion│      │ dorsal      │
│ grupo       │       │ minutos     │
└─────────────┘       │ goles       │
                      └─────────────┘

┌─────────────┐       ┌─────────────┐
│   Estadio   │       │   Partido   │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ id_sede (FK)│  └───►│ id_estadio  │
│ nombre      │       │ id_local    │
│ capacidad   │       │ id_visitante│
└─────────────┘       │ ronda       │
                      │ estado      │
                      │ marcador    │
                      └─────────────┘

┌─────────────┐       ┌─────────────┐
│   Partido   │       │  Entrada    │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ id_estadio  │  └───►│ id_partido  │
│ id_local    │       │ id_usuario  │
│ id_visitante│       │ codigo_qr   │
└─────────────┘       │ categoria   │
                      │ precio      │
                      │ estado      │
                      └─────────────┘

┌─────────────┐       ┌─────────────┐
│   Usuario   │       │   Polla     │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ id_rol (FK) │  └───►│ id_creador   │
│ nombre      │       │ codigo_acceso│
│ email       │       │ nombre       │
└─────────────┘       │ pozo_puntos  │
                      └─────────────┘

┌─────────────┐       ┌─────────────────┐
│   Polla     │       │ ParticipantePolla│
├─────────────┤       ├─────────────────┤
│ id (PK)     │──┐    │ id_usuario (PK) │
│ id_creador  │  └───►│ id_polla (PK)   │
│ nombre      │       │ puntos          │
└─────────────┘       └─────────────────┘

┌─────────────┐       ┌─────────────────┐
│   Partido   │       │   Prediccion    │
├─────────────┤       ├─────────────────┤
│ id (PK)     │──┐    │ id (PK)         │
│ id_estadio  │  └───►│ id_partido      │
│ id_local    │       │ id_usuario      │
│ id_visitante│       │ id_polla        │
└─────────────┘       │ goles_local     │
                      │ goles_visitante │
                      │ puntos          │
                      └─────────────────┘

┌─────────────┐       ┌─────────────┐
│   Usuario   │       │    Album    │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ nombre      │  └───►│ id_usuario  │
│ email       │       │ porcentaje  │
└─────────────┘       │ laminas      │
                      └─────────────┘

┌─────────────┐       ┌─────────────┐
│   Jugador   │       │   Lamina    │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ nombre      │  └───►│ id_jugador  │
│ posicion    │       │ rareza      │
│ id_seleccion│       └─────────────┘
└─────────────┘

┌─────────────┐       ┌───────────────┐
│    Album    │       │  LaminaAlbum  │
├─────────────┤       ├───────────────┤
│ id (PK)     │──┐    │ id_album (PK) │
│ id_usuario  │  └───►│ id_lamina (PK)│
└─────────────┘       │ esta_pegada   │
                      │ repetidas     │
                      └───────────────┘

┌─────────────┐       ┌─────────────┐
│   Usuario   │       │  Paquete    │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ nombre      │  └───►│ id_usuario  │
│ email       │       │ estado      │
└─────────────┘       │ fecha       │
                      └─────────────┘

┌─────────────┐       ┌─────────────────────┐
│   Usuario   │       │ IntercambioLamina   │
├─────────────┤       ├─────────────────────┤
│ id (PK)     │──┐    │ id (PK)             │
│ nombre      │  └───►│ id_solicitante      │
│ email       │       │ id_receptor         │
└─────────────┘       │ id_lamina_ofrecida  │
                      │ id_lamina_solicitada│
                      │ estado              │
                      │ fecha_solicitud     │
                      │ fecha_resolucion    │
                      └─────────────────────┘

┌─────────────┐       ┌──────────────────┐
│   Usuario   │       │IncidenteSoporte │
├─────────────┤       ├──────────────────┤
│ id (PK)     │──┐    │ id (PK)         │
│ nombre      │  └───►│ id_reportador   │
│ email       │       │ id_agente       │
└─────────────┘       │ descripcion     │
                      │ estado          │
                      │ prioridad       │
                      │ fecha_creacion  │
                      └──────────────────┘

┌─────────────┐       ┌──────────────────┐
│   Usuario   │       │LogsTransaccional│
├─────────────┤       ├──────────────────┤
│ id (PK)     │──┐    │ id (PK)         │
│ nombre      │  └───►│ id_usuario      │
│ email       │       │ accion          │
└─────────────┘       │ timestamp       │
                      │ detalle         │
                      │ hash_integridad │
                      │ nivel_riesgo    │
                      │ verificado      │
                      └──────────────────┘

┌─────────────┐       ┌─────────────┐
│   Usuario   │       │Notificacion │
├─────────────┤       ├─────────────┤
│ id (PK)     │──┐    │ id (PK)     │
│ nombre      │  └───►│ id_destinatario
│ email       │       │ id_emisor   │
└─────────────┘       │ mensaje     │
                      │ canal       │
                      │ fecha_envio │
                      └─────────────┘

┌─────────────┐       ┌────────────────────┐
│AliadoComercial│      │ReporteInteraccionAPI│
├─────────────┤       ├────────────────────┤
│ id (PK)     │──┐    │ id (PK)           │
│ nombre      │  └───►│ id_aliado         │
│ tipo_servicio│      │ endpoint          │
│ token_acceso│       │ peticiones_ok     │
│ estado      │       │ peticiones_fail   │
└─────────────┘       │ fecha_corte       │
                      └────────────────────┘

┌─────────────┐       ┌─────────────────┐
│   Usuario   │       │ReporteCompliance│
├─────────────┤       ├─────────────────┤
│ id (PK)     │──┐    │ id (PK)         │
│ nombre      │  └───►│ id_generador    │
│ email       │       │ tipo_reporte    │
└─────────────┘       │ descripcion     │
                      │ ruta_archivo     │
                      │ fecha_generacion │
                      └─────────────────┘
```

## Key Aggregates

1. **Usuario Aggregate**: Usuario → Album, Prediccion, Entrada, Paquete, Incidente, Log, Notificacion
2. **Partido Aggregate**: Partido → Entrada, Prediccion
3. **Polla Aggregate**: Polla → ParticipantePolla → Prediccion
4. **Album Aggregate**: Album → LaminaAlbum → Lamina → Jugador

## Index Recommendations

- usuarios: email (UNIQUE), id_rol
- selecciones: codigo_fifa (UNIQUE), pais (UNIQUE)
- jugadores: id_seleccion, dorsal
- partidos: fecha_hora, id_estadio
- entradas: codigo_qr (UNIQUE), id_partido, id_usuario
- pollas: codigo_acceso (UNIQUE)
- predicciones: id_usuario, id_polla, id_partido (composite unique)