spring.application.name=mundial-2026-hub
server.port=8082
server.servlet.context-path=/

# ============== Datasource (VM Remota via Tailscale) ==============
spring.datasource.url=jdbc:mysql://100.87.26.105:3306/MundialHUB2026?useSSL=false&serverTimezone=America/Bogota&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
spring.datasource.username=${DB_USER:juanita_mh}
spring.datasource.password=${DB_PASSWORD:Mundial_Jua2026}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# ============== JPA / Hibernate ==============
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.open-in-view=false
spring.sql.init.mode=never

# ============== Schema ==============
# Using Hibernate DDL (create/update) instead of schema.sql

# ============== JWT (secret BASE64-encoded, mínimo 256 bits) ==============
# Para generar uno propio: openssl rand -base64 64
jwt.secret=${JWT_SECRET:bXVuZGlhbC0yMDI2LWh1Yi1zZWNyZXQta2V5LXBhcmEtZmlybWFyLXRva2Vucy1qd3QtZGUtZm9ybWEtc2VndXJhLXkta2V5LWxhcmdhLW1pbi0zMi1ieXRlcw==}
jwt.expiration=${JWT_EXPIRATION_MS:3600000}

# ============== CORS (frontend Vite) ==============
mundial26.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177,http://localhost:4173}

# ============== Actuator ==============
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=when-authorized

# ============== Logging (con correlation ID) ==============
logging.level.root=INFO
logging.level.co.edu.unbosque=DEBUG
logging.level.org.springframework.security=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss.SSS} [%-5level] [%X{correlationId:-}] %logger{36} - %msg%n

# ============== Email (Gmail SMTP) ==============
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:mundialparatodos2026@gmail.com}
spring.mail.password=${MAIL_PASSWORD:ozkp pvku yepu moap}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000

# ============== Football Data API ==============
footballdata.api.key=${FOOTBALL_DATA_KEY:ef49dacadc7c4f97aa6650f98e8ffb87}
footballdata.api.base-url=https://api.football-data.org/v4
footballdata.competition=WC
footballdata.season=2026

# Cache (Caffeine) - 5 min TTL para respetar 10 req/min
spring.cache.type=caffeine
spring.cache.caffeine.spec=maximumSize=100,expireAfterWrite=300s

# ============== Stripe (Sandbox) ==============
# Configura las claves en variables de entorno o en application-local.properties (no commitear)
stripe.secret.key=${STRIPE_SECRET_KEY:sk_test_REEMPLAZA_CON_TU_SECRET_KEY}
stripe.publishable.key=${STRIPE_PUBLISHABLE_KEY:pk_test_REEMPLAZA_CON_TU_PUBLISHABLE_KEY}
stripe.webhook.secret=${STRIPE_WEBHOOK_SECRET:whsec_test_REEMPLAZA_CON_TU_WEBHOOK_SECRET}

# ============== Firebase FCM (Push Notifications) ==============
firebase.enabled=${FIREBASE_ENABLED:false}
firebase.service-account-json=${FIREBASE_SERVICE_ACCOUNT_JSON:}

# ============== Springdoc / Swagger ==============
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
